/* room.wxss */
@import '../../styles/common-header.wxss';

.container {
  height: 100vh;
  background: linear-gradient(180deg, #f8fffe 0%, #f0f9f4 100%);
  padding: 0;
  margin: 0;
  width: 100%;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* 房间页面头部样式调整 */

.room-status-badge {
  display: flex;
  align-items: center;
  gap: 6rpx;
  padding: 6rpx 12rpx;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 16rpx;
  backdrop-filter: blur(10rpx);
  border: 1rpx solid rgba(255, 255, 255, 0.2);
  /* 防止换行 */
  white-space: nowrap;
  /* 向右移动一些 */
  margin-left: 20rpx;
}

.room-status-badge .status-icon {
  font-size: 18rpx;
  /* 防止换行 */
  white-space: nowrap;
}

.room-status-badge .status-text {
  font-size: 18rpx;
  font-weight: 500;
  /* 防止换行 */
  white-space: nowrap;
}

/* 房间号区域 */
.room-number-section {
  padding: 20rpx 40rpx;
  text-align: center;
  background: linear-gradient(135deg, #07c160 0%, #06ad56 50%, #05a04d 100%);
  margin: 0 -40rpx 24rpx -40rpx;
  position: relative;
  overflow: hidden;
  width: calc(100% + 80rpx);
  box-sizing: border-box;
}

.room-number-container {
  position: relative;
  z-index: 2;
}

.room-number-label {
  font-size: 36rpx;
  font-weight: 600;
  color: white;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.1);
}

/* 删除重复的房间标题区域样式，使用统一的room-header */

/* 删除重复的伪元素样式 */

.room-title-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
  position: relative;
  z-index: 2;
  text-align: center;
}

.room-title-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12rpx;
}

.room-icon {
  font-size: 64rpx;
  filter: drop-shadow(0 4rpx 8rpx rgba(0, 0, 0, 0.2));
  animation: pulse 2s ease-in-out infinite;
}

.room-title {
  font-size: 48rpx;
  font-weight: 800;
  text-shadow: 0 4rpx 8rpx rgba(0,0,0,0.2);
  letter-spacing: 2rpx;
  background: linear-gradient(135deg, #ffffff 0%, #f0f9f4 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.room-status {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 12rpx 24rpx;
  background: linear-gradient(135deg, #06ad56 0%, #05a04d 100%);
  border-radius: 50rpx;
  backdrop-filter: blur(10rpx);
  border: 2rpx solid rgba(255, 255, 255, 0.3);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.2);
}

.status-dot {
  width: 16rpx;
  height: 16rpx;
  background: #ffffff;
  border-radius: 50%;
  box-shadow: 0 0 12rpx rgba(255, 255, 255, 0.8);
  animation: blink 2s ease-in-out infinite;
}

.status-text {
  font-size: 26rpx;
  font-weight: 600;
  opacity: 0.95;
  letter-spacing: 1rpx;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.6; }
}

@keyframes float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  50% { transform: translateY(-20px) rotate(180deg); }
}

.players-section {
  margin-bottom: 20rpx;
  padding: 0 16rpx 24rpx 16rpx;
  width: 100%;
  box-sizing: border-box;
  overflow: hidden;
  flex-shrink: 0;
}

.players-grid {
  display: flex !important;
  flex-wrap: wrap !important;
  width: 100%;
  box-sizing: border-box;
  padding: 0 8rpx;
}

.player-card {
  background: white;
  border-radius: 16rpx;
  padding: 20rpx 8rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
  cursor: pointer;
  border: 2rpx solid transparent;
  position: relative;
  overflow: hidden;
  min-height: 170rpx;
  width: 23%;
  margin: 1%;
  justify-content: flex-start;
  box-sizing: border-box;
}

.player-card:active {
  transform: scale(0.95);
  box-shadow: 0 6rpx 16rpx rgba(0, 0, 0, 0.12);
}

.player-card.current-player {
  border-color: #07c160;
  background: linear-gradient(135deg, #f0f9f4 0%, #e8f5e8 100%);
  box-shadow: 0 6rpx 16rpx rgba(7, 193, 96, 0.15);
}

.player-card.current-player::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 4rpx;
  height: 100%;
  background: linear-gradient(180deg, #07c160 0%, #06ad56 100%);
}

.player-avatar-container {
  position: relative;
  flex-shrink: 0;
}

.player-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: #f0f0f0;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border: 2rpx solid #f0f0f0;
  transition: all 0.3s ease;
}

.player-card.current-player .player-avatar {
  border-color: #07c160;
  box-shadow: 0 0 0 2rpx rgba(7, 193, 96, 0.2);
}

.default-avatar {
  background: linear-gradient(135deg, #e0e0e0 0%, #f0f0f0 100%);
}

.avatar-icon {
  font-size: 28rpx;
  color: #999;
}

.player-name {
  font-size: 22rpx;
  font-weight: 600;
  color: #333;
  line-height: 1.2;
  text-align: center;
  width: 100%;
  margin: 0;
  padding: 0 4rpx;
  min-height: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  word-break: break-all;
  overflow-wrap: break-word;
  hyphens: auto;
}

.player-score {
  font-size: 24rpx;
  font-weight: 700;
  color: #07c160;
  margin: 0;
  text-align: center;
  min-height: 32rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 4rpx;
}

.player-score.score-negative {
  color: #ff4757;
}

/* 空状态样式 */
.empty-players {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx 20rpx;
  background: #f8f9fa;
  border-radius: 16rpx;
  margin: 20rpx 0;
}

.empty-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
  opacity: 0.6;
}

.empty-text {
  font-size: 28rpx;
  font-weight: 600;
  color: #666;
  margin-bottom: 12rpx;
}

.empty-hint {
  font-size: 24rpx;
  color: #999;
  text-align: center;
}

.player-hint {
  font-size: 24rpx;
  color: #666;
  margin-top: 10rpx;
}

.transfer-section {
  margin-bottom: 8rpx;
  padding: 0 16rpx;
  width: 100%;
  box-sizing: border-box;
}

.transfer-hint {
  background: #f8f9fa;
  padding: 16rpx 24rpx;
  border-radius: 16rpx;
  margin: 20rpx 16rpx 8rpx 16rpx;
  color: #666;
  font-size: 26rpx;
  text-align: center;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  width: 100%;
  box-sizing: border-box;
  flex-shrink: 0;
}

.transfer-actions {
  text-align: center;
  width: 100%;
  box-sizing: border-box;
}

.transfer-actions .btn {
  width: auto;
  padding: 16rpx 32rpx;
  font-size: 28rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
}

.history-section {
  flex: 1;
  padding: 0 16rpx;
  width: 100%;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.section-title {
  display: flex;
  align-items: center;
  gap: 12rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 24rpx;
  flex-shrink: 0;
}

.transfers-scroll {
  flex: 1;
  height: 0;
  min-height: 0;
}

.history-item {
  background: white;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 16rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.06);
  transition: all 0.3s ease;
}

.history-item:active {
  transform: scale(0.98);
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

/* 与用户相关的流水 */
.history-item.related {
  background: white;
  border-left: 6rpx solid #07c160;
}

/* 与用户无关的流水 */
.history-item.unrelated {
  background: #f8f9fa;
  opacity: 0.7;
}

.history-item.unrelated .transfer-desc {
  color: #999;
}

.history-item.unrelated .transfer-time {
  color: #bbb;
}

.transfer-desc {
  font-size: 28rpx;
  color: #333;
  margin-bottom: 8rpx;
}

.transfer-time {
  font-size: 22rpx;
  color: #999;
  margin-top: 4rpx;
  opacity: 0.8;
}

.transfer-amount {
  font-size: 32rpx;
  font-weight: 600;
}

/* 用户转出的流水 - 红色 */
.transfer-amount.outgoing {
  color: #ff4757;
}

/* 别人转给用户的流水 - 绿色 */
.transfer-amount.incoming {
  color: #07c160;
}

/* 与用户无关的流水 - 灰色 */
.transfer-amount.neutral {
  color: #999;
}

/* 保留原有的positive/negative样式作为备用 */
.transfer-amount.positive {
  color: #07c160;
}

.transfer-amount.negative {
  color: #ff4757;
}

.empty-state {
  background: #f8f9fa;
  border-radius: 16rpx;
  padding: 60rpx 24rpx;
  text-align: center;
  color: #666;
  font-size: 28rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
}

.transfer-info {
  flex: 1;
}

.transfer-desc {
  font-size: 28rpx;
  margin-bottom: 5rpx;
}

.transfer-time {
  color: #666;
  font-size: 24rpx;
}

.transfer-amount {
  font-weight: 600;
  font-size: 32rpx;
}

.transfer-amount.positive {
  color: #07c160;
}

.transfer-amount.negative {
  color: #ff4757;
}

.empty-state {
  text-align: center;
  padding: 60rpx 0;
  color: #999;
  font-size: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
}

.action-buttons {
  display: flex;
  gap: 20rpx;
  margin: 0;
  padding: 20rpx 16rpx;
  width: 100%;
  box-sizing: border-box;
  background: linear-gradient(180deg, rgba(248, 255, 254, 0.8) 0%, rgba(240, 249, 244, 0.95) 100%);
  backdrop-filter: blur(10rpx);
  border-top: 1rpx solid rgba(0, 0, 0, 0.05);
  flex-shrink: 0;
}

.action-buttons .btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
}

/* 模态框样式 */
.modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.modal.show {
  opacity: 1;
  visibility: visible;
}

.modal-content {
  background: white;
  border-radius: 24rpx;
  padding: 40rpx;
  margin: 40rpx;
  max-width: 600rpx;
  width: 100%;
  max-height: 80vh;
  overflow-y: auto;
}

.modal-header {
  text-align: center;
  margin-bottom: 40rpx;
  position: relative;
}

.modal-title {
  font-size: 36rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
}

.close-btn {
  position: absolute;
  top: 0;
  right: 0;
  font-size: 40rpx;
  color: #666;
  cursor: pointer;
}

.share-room-info {
  margin: 40rpx 0;
}

.share-room-info .room-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 20rpx;
  gap: 10rpx;
}

.share-room-info .room-id {
  font-size: 36rpx;
  font-weight: 600;
  color: #07c160;
}

.share-room-info .status-badge {
  background: #07c160;
  color: white;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  font-size: 28rpx;
  font-weight: 600;
}

.share-room-info .room-stats {
  color: #666;
  font-size: 28rpx;
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.qr-code-section {
  margin: 40rpx 0;
}

.qr-code-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.qr-code-header .icon {
  margin-right: 10rpx;
}

.btn-small {
  padding: 12rpx 24rpx;
  font-size: 24rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  border: none;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.loading-indicator {
  display: flex;
  align-items: center;
  gap: 8rpx;
  color: #667eea;
  font-size: 24rpx;
}

.loading-indicator .icon {
  animation: spin 1s linear infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.qr-code-display {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
}

.qr-code-image {
  width: 300rpx;
  height: 300rpx;
  border-radius: 24rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.1);
}

.qr-code-hint {
  display: flex;
  align-items: center;
  gap: 10rpx;
  color: #666;
  font-size: 28rpx;
}

.qr-code-placeholder {
  width: 300rpx;
  height: 300rpx;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  border-radius: 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  margin: 0 auto;
  color: #666;
  font-size: 28rpx;
  gap: 20rpx;
  border: 2rpx dashed #ccc;
}

.share-actions {
  display: flex;
  gap: 20rpx;
  margin: 40rpx 0;
}

.share-actions .btn {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
}

.modal-footer {
  text-align: center;
  margin-top: 30rpx;
}

.modal-footer .btn {
  width: auto;
  padding: 16rpx 40rpx;
  font-size: 28rpx;
}

.share-instructions {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  padding: 24rpx;
  background: #f8f9fa;
  border-radius: 16rpx;
  margin: 20rpx 0;
  border: 2rpx solid #e9ecef;
}

.instruction-item {
  display: flex;
  align-items: center;
  gap: 12rpx;
  color: #333;
  font-size: 28rpx;
  line-height: 1.4;
}

.instruction-item .icon {
  font-size: 32rpx;
  color: #07c160;
}

/* 个人信息浮窗样式 */
.profile-form {
  padding: 20rpx 0;
}

.form-group {
  margin-bottom: 32rpx;
}

.form-label {
  display: block;
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 16rpx;
}

.avatar-upload-btn {
  background: none;
  border: none;
  padding: 0;
  margin: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 120rpx;
  height: 120rpx;
  line-height: normal;
  font-size: inherit;
  border-radius: 50%;
  overflow: hidden;
  position: relative;
}

.avatar-upload-btn::after {
  border: none;
}

.avatar-btn-hover {
  opacity: 0.8;
  transform: scale(0.95);
}

.avatar-image {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  object-fit: cover;
  display: block;
  flex-shrink: 0;
}

.avatar-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 120rpx;
  height: 120rpx;
  background: #f5f5f5;
  border-radius: 50%;
  border: 2rpx dashed #ddd;
}

.placeholder-icon {
  font-size: 32rpx;
  margin-bottom: 8rpx;
  opacity: 0.6;
}

.placeholder-text {
  font-size: 20rpx;
  color: #999;
  text-align: center;
}

.avatar-hint {
  display: block;
  font-size: 24rpx;
  color: #999;
  text-align: center;
  margin-top: 12rpx;
}

.form-input {
  width: 100%;
  padding: 20rpx 24rpx;
  border: 2rpx solid #e0e0e0;
  border-radius: 12rpx;
  font-size: 28rpx;
  color: #333;
  background: #fff;
  box-sizing: border-box;
}

.form-input:focus {
  border-color: #07c160;
  outline: none;
}

/* 个人信息浮窗样式 - 与主页保持一致 */
.profile-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.profile-modal-content {
  background: white;
  border-radius: 20rpx;
  width: 90%;
  max-width: 600rpx;
  max-height: 80vh;
  overflow: hidden;
  box-shadow: 0 20rpx 60rpx rgba(0, 0, 0, 0.2);
}

.profile-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 40rpx 40rpx 20rpx;
  border-bottom: 2rpx solid #f0f0f0;
}

.profile-modal-title {
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
}

.profile-modal-close {
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: #f5f5f5;
}

.close-icon {
  font-size: 40rpx;
  color: #666;
  line-height: 1;
}

.profile-modal-body {
  padding: 40rpx;
}

.profile-modal .form-group {
  margin-bottom: 40rpx;
}

.profile-modal .form-label {
  display: block;
  font-size: 28rpx;
  color: #333;
  margin-bottom: 16rpx;
  font-weight: 500;
}

.profile-modal .form-input {
  width: 100%;
  padding: 24rpx 20rpx;
  border: 2rpx solid #e0e0e0;
  border-radius: 16rpx;
  font-size: 32rpx;
  background: #fff;
  box-sizing: border-box;
  transition: all 0.3s ease;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.profile-modal .form-input:focus {
  border-color: #07c160;
  background: #fff;
  box-shadow: 0 4rpx 16rpx rgba(7, 193, 96, 0.15);
  outline: none;
}

.profile-modal .form-input::placeholder {
  color: #999;
  font-size: 28rpx;
}

.avatar-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
}

.avatar-hint {
  font-size: 24rpx;
  color: #999;
  text-align: center;
}

.profile-modal-footer {
  display: flex;
  gap: 20rpx;
  padding: 20rpx 40rpx 40rpx;
  border-top: 2rpx solid #f0f0f0;
}

.profile-modal .cancel-btn {
  flex: 1;
  background: #f5f5f5;
  color: #666;
  border: none;
  padding: 24rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.profile-modal .save-btn {
  flex: 1;
  background: #07c160;
  color: white;
  border: none;
  padding: 24rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
}

/* 结算浮窗样式 */
.settlement-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.settlement-modal-content {
  background: white;
  border-radius: 24rpx;
  width: 90%;
  max-width: 600rpx;
  max-height: 80vh;
  overflow: hidden;
  box-shadow: 0 20rpx 60rpx rgba(0, 0, 0, 0.3);
}

.settlement-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 40rpx 40rpx 20rpx;
  border-bottom: 2rpx solid #f0f0f0;
}

.settlement-modal-title {
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.settlement-modal-close {
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: #f8f9fa;
  transition: all 0.3s ease;
}

.settlement-modal-close:active {
  background: #e9ecef;
  transform: scale(0.95);
}

.settlement-modal-body {
  padding: 30rpx 40rpx;
  max-height: 60vh;
  overflow-y: auto;
}

.settlement-summary {
  text-align: center;
  margin-bottom: 30rpx;
  padding: 20rpx;
  background: #f8f9fa;
  border-radius: 12rpx;
}

.summary-text {
  font-size: 28rpx;
  color: #666;
  font-weight: 500;
}

.settlement-list {
  margin-bottom: 30rpx;
}

.settlement-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 20rpx;
  margin-bottom: 16rpx;
  background: #f8f9fa;
  border-radius: 16rpx;
  border-left: 6rpx solid #07c160;
}

.settlement-info {
  flex: 1;
}

.settlement-direction {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 8rpx;
  font-size: 28rpx;
}

.from-user {
  color: #ff4757;
  font-weight: 600;
}

.arrow {
  color: #666;
  font-size: 24rpx;
}

.to-user {
  color: #07c160;
  font-weight: 600;
}

.settlement-amount {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 24rpx;
}

.amount-label {
  color: #666;
}

.amount-value {
  color: #333;
  font-weight: 600;
}

.settlement-number {
  width: 40rpx;
  height: 40rpx;
  background: #07c160;
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  font-weight: 600;
  flex-shrink: 0;
}

.settlement-hint {
  display: flex;
  align-items: center;
  gap: 12rpx;
  padding: 20rpx;
  background: #e8f5e8;
  border-radius: 12rpx;
  color: #07c160;
  font-size: 26rpx;
}

.empty-settlement {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60rpx 20rpx;
  text-align: center;
}

.empty-settlement .icon {
  font-size: 64rpx;
  margin-bottom: 20rpx;
  opacity: 0.6;
}

.empty-settlement .empty-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #666;
  margin-bottom: 12rpx;
}

.empty-settlement .empty-hint {
  font-size: 26rpx;
  color: #999;
  line-height: 1.4;
}

.settlement-modal-footer {
  padding: 20rpx 40rpx 40rpx;
  border-top: 2rpx solid #f0f0f0;
}

.confirm-btn {
  width: 100%;
  padding: 24rpx 32rpx;
  background: linear-gradient(135deg, #07c160, #06ad56);
  color: white;
  border-radius: 16rpx;
  font-size: 32rpx;
  font-weight: 500;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  border: none;
  transition: all 0.3s ease;
}

.confirm-btn:active {
  transform: translateY(2rpx);
  box-shadow: 0 2rpx 8rpx rgba(7, 193, 96, 0.3);
}



/* 已结算房间玩家卡片样式 */
.player-card.settled-room {
  opacity: 0.8;
  position: relative;
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
  border: 2rpx solid #dee2e6;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
}

.player-card.settled-room::after {
  content: '🏁';
  position: absolute;
  top: 12rpx;
  right: 12rpx;
  font-size: 28rpx;
  background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
  color: white;
  border-radius: 50%;
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10;
  box-shadow: 0 4rpx 12rpx rgba(40, 167, 69, 0.3);
  border: 2rpx solid rgba(255, 255, 255, 0.8);
}

.player-card.settled-room .player-name {
  color: #6c757d;
  font-weight: 500;
}

.player-card.settled-room .player-score {
  color: #495057;
  font-weight: 600;
}

/* 房间状态样式 */
.room-status {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-top: 12rpx;
  padding: 8rpx 16rpx;
  border-radius: 20rpx;
  font-size: 24rpx;
  font-weight: 500;
}

.room-status.settled {
  background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
  color: white;
  box-shadow: 0 4rpx 12rpx rgba(40, 167, 69, 0.3);
}

.room-status .status-icon {
  font-size: 26rpx;
}

.room-status .status-text {
  font-size: 24rpx;
  font-weight: 600;
}

/* 已结算房间提示样式 */
.settled-room-notice {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  margin: 20rpx 0;
  padding: 16rpx 24rpx;
  background: linear-gradient(135deg, #e3f2fd 0%, #f3e5f5 100%);
  border: 2rpx solid #bbdefb;
  border-radius: 24rpx;
  box-shadow: 0 4rpx 12rpx rgba(33, 150, 243, 0.1);
}

.settled-room-notice .notice-icon {
  font-size: 32rpx;
}

.settled-room-notice .notice-text {
  font-size: 28rpx;
  color: #1976d2;
  font-weight: 500;
}

/* 空状态提示样式 */
.empty-state .empty-hint {
  font-size: 24rpx;
  color: #6c757d;
  margin-top: 8rpx;
  font-style: italic;
}
